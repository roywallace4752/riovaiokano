name: IR_SSH_Shahan_Style_2026_Playit
on:
  workflow_dispatch:

jobs:
  deploy_ssh:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    
    steps:
      - name: Setup SSH Environment
        run: |
          sudo apt-get update
          sudo apt-get install -y openssh-server sshpass jq curl gpg
          
          # ایجاد کاربر
          sudo useradd -m -s /bin/bash shahan_user
          echo "shahan_user:pass1234" | sudo chpasswd
          
          # استارت سرویس SSH
          sudo service ssh start

      - name: Install Playit.gg Agent
        run: |
          # رفع خطای Malformed entry با افزودن فاصله قبل از./
          curl -SsL https://playit-cloud.github.io/ppa/key.gpg | gpg --dearmor | sudo tee /etc/apt/trusted.gpg.d/playit.gpg >/dev/null
          echo "deb [signed-by=/etc/apt/trusted.gpg.d/playit.gpg] https://playit-cloud.github.io/ppa/data./" | sudo tee /etc/apt/sources.list.d/playit-cloud.list
          sudo apt update
          sudo apt install -y playit

      - name: Start Playit Tunnel
        env:
          SECRET_KEY: ${{ secrets.PLAYIT_SECRET_KEY }}
        run: |
          # اجرای کلاینت در حالت بدون گرافیک با استفاده از کلید مخفی
          playit --no-gui > playit.log 2>&1 &
          
          # انتظار برای برقراری اتصال
          sleep 20
          
          echo "======================================"
          echo "✅ SSH Server is live via Playit.gg"
          
          # بررسی وضعیت اتصال در لاگ 
          if grep -q "secret key valid" playit.log; then
            echo "Authentication: SUCCESS"
          else
            echo "Authentication: FAILED - Please check your PLAYIT_SECRET_KEY"
            # نمایش بخشی از لاگ برای عیب‌یابی در صورت شکست
            tail -n 5 playit.log
            exit 1
          fi
          echo "======================================"

      - name: Connection Information
        run: |
          echo "======================================"
          echo "User: shahan_user"
          echo "Pass: pass1234"
          echo "نکته: آدرس و پورت ثابت خود را از پنل Playit.gg دریافت کنید."
          echo "======================================"

      - name: Keep Runner Alive
        run: |
          echo "Runner will stay active for 6 hours."
          # حلقه برای باز نگه داشتن محیط و نمایش وضعیت [10]
          for i in {1..360}; do
            echo "Heartbeat $i: Connection Active..."
            sleep 60
          done
